# 2026-09-18 财务会议需求实施计划

目标：完成项目说明书 12.21 节及用户逐项确认的 23 项需求。基线 `ae16c38`，实施分支 `feat/finance-meeting-20260918`。

约束：保留真实业务记录、Finalized 金额、HWM、账单编号、历史附件和持仓 JSON；仅新导出 Excel 计划列移除 Code。全部测试先用 F 盘隔离合成数据，正式切换遵守 AGENTS.md 的备份、预检和恢复验证。

## 步骤与验收

- [ ] 后端资金、结余与基础资料（B1/B3/B4/E1/E2/E3）：新增月供类别，历史 CONTRIBUTION 保留存储并显示加款；两类流入共同计算。附件与新记录原子关联，三类资金及手工结余无凭证拒绝；更正保留历史凭证并审计。删除 FC/FeePlan Code 与独立 Closing 资格依赖，保留日期、账户、期间和原件校验；迁移前后冻结金额一致。
- [ ] 识别与复核（B2/P5/P6）：移除 OCR/Sol 持仓提取和数量差异流程，保留身份、日期、币种、金额的人工复核；原历史持仓不改写。导入组件供客户及资金页复用。
- [ ] 客户与概览（N6/P1/P2/P3/P5/P8）：概览在管数字携年季跳转，客户页按实际管理期间承接列表；共享客户/年季/FC/收费计划筛选，保留档案维护能力。
- [ ] 资金页面、导航及全站显示（N1–N5/P4/P6/P7/P8/E1–E4/F1）：三组布局、统一提交、原始凭证弹窗、适用期初结余、中文日期、复合身份同名去重；取消独立导入导航。
- [ ] 集成复核：逐项审查清单，运行完整后端、前端测试及构建，验证迁移/拒绝/并发/失败一致性与导出配套变化，使用合成数据做实际浏览器及 EXE 验收。
- [ ] 文档、发布与交付：同步说明书、操作手册、变更记录及版本；完整备份和隔离恢复验证后按持续授权升级正式版，核对数据和文件保留，Git 同步及精确清理。

接口协调：前端后端使用 `MONTHLY_CONTRIBUTION` 表示供款，既有 `CONTRIBUTION` 表示加款，`WITHDRAWAL` 表示取款；不改写已锁定历史流水。新记录使用 `attachment_ids` 关联已上传的未认领凭证，成功记账须在同一数据库事务校验、认领全部凭证。人工更正可选新附件且保留旧附件审计。实现如需调整字段，由主代理统一协调。
