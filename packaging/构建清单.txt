金融计划收费计算系统 MVP 0.2.7 Windows运行版构建清单
基线日期：2026-08-28

构建环境：
- Python 3.12.13
- Node.js 24.19.0（仅构建前端，发布包运行不需要Node.js）
- pnpm 11.19.0
- Python包：见 backend/requirements-dev-lock.txt
- Node包：见 frontend/pnpm-lock.yaml

内置运行组件：
- OpenAI Codex CLI 0.149.1
- Tesseract OCR 5.5.3.20260724

关键输入SHA-256：
- codex.exe: A395030B56B126F608F2403036DDDB654A9C063213E9C2B5F85D954CF490EBE6
- codex-code-mode-host.exe: 8F98CC7AA079B51DBFBB16A8E655A468A9C37C1CD23E22422C10CDFD6CACE543
- tesseract.exe: C66F0F12ED76F6AA455DAC97684BBC86756D6A732380BEE09122454CFDA3F420
- 新收费计划计算.xlsx: 59A1C381BFA82CBE814BDF1C4821E49EBE8D2579DFDD58A3A8A9BF18F84E5BF6

构建输出规则：
- 每次只生成`release/FinancialFeeSystem`，构建开始时删除上一份`release`，不累计候选版本目录。
- 源码回归：后端完整测试、前端TypeScript及生产构建均须通过后才生成运行包。
- 两个Codex可执行文件必须同时通过固定版本、SHA-256及OpenAI Authenticode签名校验；任一不符立即停止构建。
- `构建结果.txt`由构建脚本在发布目录内自动生成，记录当次时间、EXE哈希、文件数、字节数和Windows版本，不再手工复制上一版结果。
- 候选包只用于隔离验收；正式提升完成后删除候选目录、`backend/build`、`backend/*.spec`和`frontend/dist`。
- 本机只保留最新正式运行版；业务数据和完整备份继续独立保存，不随运行版清理。
- 隔离验收不得读取真实客户文件或调用Luna额度，除非用户另行明确授权。

重新安装依赖、更新模板或替换任一内置程序后，必须重新生成本清单并执行完整验收。
