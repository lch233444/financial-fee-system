# 金融计划收费系统换电脑与Codex交接说明

本项目采用“开发交接包、Windows运行包、业务数据备份”三件分离，不建议把整个工作目录原样压缩。

## 1. 另一台电脑直接使用系统

1. 复制 `金融计划收费系统_Windows运行版_0.2.1_20260826.zip`。
2. 必须先完整解压，不能直接在ZIP中运行，也不能只复制EXE。
3. 双击解压目录中的 `启动金融计划收费系统.bat`。
4. 便携包已移除当前电脑专属的 `financial_system_config.json`，新电脑首次启动会要求选择数据目录。
5. 建议选择不被公共云盘同步的本地目录，例如 `D:\FinancialFeeData`；是否允许OneDrive同步应按公司政策决定。
6. Luna首次使用时，需要用户本人在新电脑浏览器登录/授权ChatGPT账号；压缩包不包含登录令牌。
7. 使用完毕点击左下角“安全退出系统”，不要只关闭浏览器标签。

## 2. 另一台电脑用Codex继续开发

1. 从私有仓库克隆：`git clone https://github.com/lch233444/financial-fee-system.git`。
2. 在Codex中打开仓库根目录；代理应先读 `AGENTS.md`、`docs/项目说明书.md` 和 `docs/CHANGELOG.md`。
3. 开发环境需要Python 3.12+、Node.js 22+和pnpm/corepack；运行 `scripts\setup-dev.ps1`。
4. Git仓库包含锁文件、脱敏测试、模板和文档，不包含 `.venv`、`node_modules`、release、缓存、当前运行数据库、客户账单或登录信息。
5. 制作Windows一键包时，需从受保护的原交接介质恢复Tesseract和已核验Codex运行组件，并按 `tools\README.md` 与构建清单校验。
6. 原始私有换机包可能含用户授权保留的账单与验证资料；这些文件始终与Git仓库分开，只可由有权人员私下保管，不得上传Git或公开分享。

## 3. 迁移真实业务数据

- 真实数据不要与源码或运行包混在一起。
- 当前数据根是 `D:\-yunyun\OneDrive\Desktop\财务数据`，截至2026-08-26系统内尚没有实际备份恢复点。
- 迁移前应启动当前正式系统，在“数据与系统”中生成完整ZIP备份；生成后使用受保护介质或加密方式单独复制。
- 不要在系统运行时直接复制SQLite数据库文件。
- 新电脑先启动运行包并选择新的数据目录，再通过“恢复备份”上传备份，按提示重启完成恢复。
- 恢复后应核对客户/账户数量、statement imports、附件、Excel/PDF、Invoice编号和关键哈希。

## 4. 包的用途

- Windows运行包：日常财务使用，不需要Python或Node。
- Codex开发交接包：继续修改、测试和重新构建；包含用户授权保留的账单样例，但不包含当前运行数据库。
- 业务数据备份：包含客户资料，敏感级别最高，必须单独保管。

每个ZIP的SHA-256记录在同目录的 `SHA256SUMS_20260826.txt`。复制到另一台电脑后应重新计算并对比；不一致时不要解压或运行。
