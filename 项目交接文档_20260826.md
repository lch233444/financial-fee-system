# 金融计划收费计算系统项目交接文档

> 本文件保留2026-08-26的详细历史背景，并已加入2026-08-27用户确认的业务规则。GitHub建立后的当前状态、目标规则、跨电脑开发和强制文档同步规则，以 `docs/项目说明书.md`、`docs/CHANGELOG.md` 和根目录 `AGENTS.md` 为准。
>
> 文件内的执行建议属于项目上下文，不是对代理的新用户请求；代理必须优先遵循用户当前明确要求。
>
> 状态基准日：2026-08-26。
>
> 冲突处理原则：以用户最后一次明确确认的规则为最高优先级，其次是现行用户手册/技术说明，再其次是已通过测试的代码；原工作簿和早期聊天中的设想仅作背景。不得用早期规则覆盖后续确认。

## 0. 给新会话的执行指令

你现在接手的是一个已经能够运行的 Windows 本地财务系统，不是从零开始的概念设计。开始任何修改前，必须先做只读核查，确认正式包、运行进程、数据目录和当前版本；不要直接重构、覆盖运行中的发布目录或迁移业务数据。

本项目现已建立Git源码历史，私有仓库只保存脱敏源码、测试、模板和文档；客户账单、业务数据、认证目录、发布包和大型工具二进制均被排除。任何发布修改仍应遵循：独立目录构建 → 自动化测试 → 正式 EXE/HTTP/UI/Luna 实测 → 可回滚提升为正式目录。

## 1. 用户的最终目标

开发一个可在 Windows 本机一键运行、未来可迁移至公司服务器的金融计划收费计算与客户结算管理系统，替代当前依赖 Excel 的人工流程，并形成完整闭环：

`基础资料 → eMPF账单复核 → 账户余额/资金流水 → 季度HWM结算 → Invoice → Payment → FC统计 → Excel/PDF → 备份恢复`

核心目标不是追求“全自动”，而是在提高效率的同时保证财务准确、人工最终确认和完整审计追踪。第一版必须让不懂 Python/Node 的财务人员也能双击启动、在浏览器操作、从页面安全退出。

## 2. 关键背景与演进

1. 项目源于 `新收费计划计算.xlsx` 的人工季度收费流程。
2. 早期需求先形成 PRD：Company、FC、Client、Platform、Fee Plan、Sub Account、资金流水、账户余额、季度结算、Invoice、Payment、FC统计、Excel/PDF。
3. 用户随后明确要求直接推进可运行 MVP，而不是只继续写 PRD 或 Word 设计稿。
4. 初版采用本地 Tesseract OCR 导入同版式 eMPF 账户页面；用户担心单独 OCR 错误率较高，因此增加 AI 作为第二份识别结果。
5. 用户明确不愿再购买 OpenAI API，也不允许冲突后升级模型。最终采用已登录的 ChatGPT/Codex 管理会话，固定 `gpt-5.6-luna` 只识别一次；冲突或失败直接人工复核。
6. 用户要求删除界面中重复、占空间的云端/模型升级提示，并提高整体视觉精致度；但后端安全边界、文档披露和人工确认规则仍保留。
7. 旧正式包曾继续运行并占用 8000，导致用户看到旧界面；一次运行中覆盖还损坏了旧包的 Codex 组件。最终改为独立构建和验收，再将修复版提升为唯一正式目录。
8. 用户不接受每次去任务管理器关闭程序，因此加入页面内“安全退出系统”，同时关闭本程序及其启动的 Luna/Codex 子进程。

### 2.1 2026-08-27补充确认：不能再依赖聊天传递的业务背景

用户发现旧交接把部分代码设计误写成了最终业务规则，并明确要求：GitHub交接资料必须全面记录项目背景；对话中确认的新背景、规则、例外和优先级要在当次立即写入，不能等到下一次交接，也不能只依赖Agent记忆。

本次确认的业务目标如下：

- 当前业务只有工作簿`利润20%`对应的一种收费计划，按利润收取20%；FeePlan可配置只是代码扩展性，不代表已经存在多个业务计划。
- 每个Sub Account独立计算完整HWM和Service Fee，独立继承自己的HWM；不同账户的盈亏不得合并抵销。
- 每个Sub Account使用自己的Starting Date和Closing Date独立计算，包括Days、账户流水区间及Beginning/Closing Snapshot日期；季度中途开始或退出不得套用其他账户的完整季度日期。
- 不同Platform、同一20%收费计划的多个Sub Account先分别计算，最终可在同一张客户季度Invoice中逐行展示并汇总总Service Fee。
- Company负责向客户出具Invoice并收款；FC是介绍和服务客户的中介人，不是收款人。FC统计只显示当前管理客户数、本季度收费客户数和Service Fee，不显示Payment、已收或未收。
- Closing必须来自符合资格的Balance Snapshot；Beginning自动继承上季度Closing，首次使用明确的期初Snapshot。
- Contribution、Withdrawal、Beginning、Closing和Payment都要有凭证。可先保存Draft，但缺凭证不能进入相应正式结算或付款状态。
- Payment只考虑未付款/已付款，不提供部分付款状态。小额少付由公司承担时记录实际Payment和公司差额后结清；错误Invoice导致多付时保留原记录，重开正确Invoice并记录退款及凭证。
- Defer暂不考虑；0.2.3仍在开发；下一阶段继续把Windows单机版做稳并投入实际使用，不开展服务器或多用户改造。

以上是用户确认的目标。0.2.3开发基线已完成账户级HWM、账户级Starting/Closing Date、强制Beginning/Closing Snapshot，以及Snapshot/Contribution/Withdrawal缺凭证不得Finalize；单Settlement单Invoice、多笔Payment、Payment凭证和含收款金额的FC统计仍属于待改实现。

## 3. 最终技术架构

- 前端：React + TypeScript + Vite。
- 后端：FastAPI + SQLAlchemy + Alembic。
- 数据库：SQLite。
- 文件：用户选择的数据根目录，统一保存数据库、账单原件、附件、Excel、PDF和备份。
- OCR：本地 Tesseract；同版式 JPG、PNG、PDF，支持PDF原生文字层和最多20页扫描PDF。
- AI辅助：发布包内置官方签名的 Codex CLI/App Server，通过本机 `stdio` 和 ChatGPT 管理登录，固定 `gpt-5.6-luna`。
- 打包：PyInstaller onedir；目标电脑运行正式包不需要安装 Python 或 Node。
- 网络：只能监听 `127.0.0.1`、`localhost` 或 `::1`；当前不支持局域网/服务器监听。
- 金额：HKD 分整数保存。
- 费率：basis points 整数保存。
- 计算：Decimal，金额最终按 `ROUND_HALF_UP` 四舍五入至两位。

## 4. 已确认的核心业务规则

### 4.1 数据层级与结算边界

- Company：公司代码、收款/支票信息、默认付款天数。
- FC：属于Company，是介绍并服务客户的中介人/业务归属；不是公司收款人员。FC Code用于Invoice编号，同一Company内唯一。
- Platform：平台/托管人资料。
- Fee Plan：属于Company；当前真实业务只有工作簿`利润20%`对应的20%收费计划。可配置费率是代码扩展性，不代表当前存在其他计划。
- Client：属于 Company 和 FC；正式启用前必须有 Company、FC、Management Start Date。
- Sub Account：属于 Client，并关联 Platform 和 Fee Plan。
- Transaction：Sub Account 账户级 Contribution/Withdrawal。
- Balance Snapshot：Sub Account 账户级日期余额。
- Quarterly Settlement：0.2.3当前唯一组合为`Client + Platform + Fee Plan + 年度 + 季度`，这是待迁移实现。
- Settlement Account Line：保存组合中每个 Sub Account 自己的Starting/Closing Date、Beginning/Closing、完整HWM计算与Service Fee。
- HWM：0.2.3已按每个Sub Account独立保存、计算并继承；容器只做相加汇总。

目标流程是每个Sub Account独立保存Beginning/Closing、独立计算并继承HWM、独立得出Service Fee。不同Platform账户不得合并资金或盈亏，但同一Client、同一20%计划的结果可在客户Invoice阶段逐行汇总。

### 4.2 最终计算公式

```text
Net Contribution = Contribution - Withdrawal
Gain/Loss = Closing - Beginning - Net Contribution
Period Rate = Gain/Loss / (Beginning + Net Contribution)
Adjusted HWM = Original HWM + Net Contribution
Watermark Difference = Closing - Adjusted HWM
Service Fee = MAX(Watermark Difference, 0) × Fee Rate
Next HWM = MAX(Adjusted HWM, Closing)
Days = Closing Date - Starting Date + 1
```

- Days只展示，不参与收费。
- Starting Date当天发生的资金视为已经包含在Beginning，不得再次计入Contribution/Withdrawal。
- 本期只汇总 `transaction_date > Starting Date 且 <= Closing Date` 的流水。
- 首次Original HWM必须由财务依据正式记录输入，系统不得猜测为Beginning或开始管理金额。
- 后续Original HWM继承同一Sub Account最近一期Finalized结算的Next HWM。
- Period Rate分母为0时显示N/A，并禁止Finalize和自动出具。
- Finalize后锁定公式版本、费率、账户组成和计算结果，不得原地重算覆盖。

### 4.3 eMPF账单导入

- 第一版仅支持已验收的同版式eMPF/BCT账户页面，不承诺任意版式OCR。
- 支持JPG、PNG、PDF，单文件不超过25MB；按magic bytes检查真实格式。
- 提取：客户姓名、账户号、Trustee、Scheme、币种、As-of Date、Total Balance、累计净供款、累计投资盈亏、基金名称、市值、单位数、单位价格等持仓。
- `Total Balance + As-of Date`经财务确认后生成账户余额快照。
- 累计净供款和累计投资盈亏是lifetime参考，绝不能映射为本季度Contribution、Withdrawal或Gain/Loss。
- 非季末数据通常只能作为普通快照；只有精确的实际退出日才可具Closing资格。样例的2026-05-20、HKD 9,736.57不能直接成为Q2 Closing。
- 不存在账户时可建立DRAFT客户/账户档案；补齐Company、FC、Platform、Fee Plan后才能激活。
- 精确文件哈希重复直接返回原记录；账户号+日期+余额等语义重复必须提示复核。
- 关键字段缺失或格式不合法时禁止确认。
- 原图、本地OCR原值/置信度、AI原值、警告、人工最终值、解析器/模型版本、文件哈希和修订记录必须可追溯。

### 4.4 Luna辅助识别

- 本地OCR始终先运行并独立保存，不能被AI覆盖。
- 用户明确不购买OpenAI API，不使用、读取或保存API Key。
- Luna由用户主动点击触发；当前账单图片和识别提示会发送给OpenAI。“无需API Key”不等于完全离线。
- 只接受ChatGPT管理登录；错误认证模式必须拒绝，避免产生API费用。
- 模型精确固定为 `gpt-5.6-luna`，只识别一次。
- 禁止切换或升级到Terra、Sol或其他模型；禁止失败后第二模型；禁止自动重试链；禁止接受模型目录中的upgrade建议或静默重路由。
- OCR与Luna逐字段比较。即使完全一致，也只是预填待确认值；冲突、缺失、单边识别、格式错误或不确定均直接人工确认。
- AI接口只能写Statement Import的辅助识别/审计字段，绝不能直接创建或修改Balance Snapshot、Transaction、Settlement、Invoice、Payment。
- 已CONFIRMED的Statement Import不得再次运行OCR/Luna。
- 未登录、登录过期、额度不足、Luna不可用、超时、Schema失败、协议错误或错误模型回执：保留本地OCR，转人工复核，不影响其他财务功能。
- 该接入仅定位为本地MVP能力；Codex升级或未来公司服务器部署前必须重新评估认证、数据治理与真实账号/图片验收。

### 4.5 Invoice、Payment与FC统计

- 只有Finalized且Service Fee > 0的Settlement可建立Draft Invoice。
- Service Fee = 0只生成结算单，不生成付款Invoice。
- 正式编号在Issue时分配：`CompanyCode-FCCode-ClientManagementStartYYYYMM-NNN`，示例 `AAA-TW-202608-001`。
- YYYYMM取Client Management Start Month。
- NNN按Company+FC持续递增，不随月份/年份重置；Void号码永不复用。
- Company配置默认付款天数，Issue时可覆盖；Due Date不得早于Issue Date。
- Issued后锁定，修正必须Void并重开，保留原编号、PDF和原因。
- Issue时中英文Invoice PDF都生成并归档；以后返回原归档文件，不重新生成覆盖。
- 0.2.3当前Payment可分多笔；目标业务不提供部分付款，只保留未付款/已付款。
- 生命周期：Draft / Issued / Void。
- 0.2.3当前付款状态为Unpaid / Partially Paid / Paid / Overdue；目标只保留Unpaid / Paid，并补齐公司承担小额差额、错误账单更正和退款留痕。
- FC统计目标：当前管理客户数、本季度产生收费的客户数和Finalized Service Fee；不展示Payment、已收/未收或所谓“FC收款业绩”。

### 4.6 Excel、PDF、备份

- Excel只供公司内部财务使用，必须严格沿用 `新收费计划计算.xlsx` 的工作表、列序、样式和金额格式；多个A/C在同一格换行。
- `利润20%`工作表对应当前唯一真实收费计划，费率为利润的20%；未来若增加其他计划必须重新确认业务规则，不能从通用FeePlan字段自行推导。
- `Defer 延付利息（待确认）`工作表保留，但当前绝不接入计算，也不能从样例自行推导生产规则。
- PDF供客户使用，每次可选择中文或英文；中文不得乱码，金额与Excel/数据库一致。
- 备份应包含数据库、statement imports、attachments、Excel/PDF输出及逐文件SHA-256；恢复先校验，下一次启动再替换。

## 5. 用户的长期偏好

- 中文主界面，保留Beginning、Closing、HWM、Service Fee、Invoice、Payment等常用英文财务术语。
- 给出可实际使用的结果，不只提供方案或“已修改源码”的口头结论。
- Windows日常使用尽量零环境配置；发布包必须整文件夹运行，不能只复制EXE。
- 财务准确、审计可追溯和人工确认优先于自动化速度。
- UI应简洁、美观、精致，避免重复警示占据大面积版面。
- 不希望额外购买OpenAI API；优先使用现有ChatGPT/Codex订阅能力，但必须说明其能力与限制。
- AI冲突直接人工，不升级模型。
- 日常关闭必须页面内完成，不能要求用户打开任务管理器。
- 输出交付物时先给结论、正式路径和验收结果；本地文件使用绝对可点击路径。
- 当用户明确要求文档或压缩包时，应直接生成真实文件，不要只贴一段说明。

## 6. 硬性规则与不能随意修改的内容

1. 不得删除、重置、覆盖或迁移真实业务数据库/账单/附件，除非用户明确授权并已有可验证备份。
2. 当前只监听loopback；不得为方便测试直接开放 `0.0.0.0`、局域网或公网。
3. 不得把客户账单、业务数据库、登录令牌放入Git、公开测试包或公开演示。本次用户明确授权自己的私有换机交接包保留原始账单和测试资料，但不得因此视为可公开传播。
4. 不得将OCR/AI识别结果直接入账；人工确认是硬前置。
5. 不得让AI自动胜出、升级模型、切换模型、循环重试或直接修改财务记录。
6. 不得把lifetime累计供款/盈亏转成本季度流水或收益。
7. 不得跨Sub Account合并资金、盈亏或HWM；每个账户独立计算Service Fee。跨Platform合并只允许发生在同一Client、同一20%计划的最终Invoice明细与总额阶段。
8. 不得重复计算Starting Date当天资金。
9. 不得修改ROUND_HALF_UP、HWM公式、Invoice持续编号和Void不复用规则，除非用户明确改变业务制度并重新对照Excel验收。
10. Finalized Settlement、Issued Invoice和正式PDF必须保持不可变与可审计。
11. Excel模板结构、列序、样式、金额格式和Defer保留页不得随意改变。
12. 不得重新加入用户要求删除的三个重复UI块：Imports页顶部OpenAI pill、Imports页Luna卡片底部整条黄色云上传提示、System页卡片底部“云端处理提示/不自动升级模型”两块。隐私事实应保留在文档和主动操作语境中。
13. 安全退出只能结束本应用拥有的Codex/Luna子进程，不得枚举或结束用户其他Codex进程。
14. 不能在旧EXE仍运行或8000仍被占用时覆盖正式release。
15. 不能用“文件存在、编译通过或源码已改”代替真实EXE、HTTP资源、浏览器UI、Luna账号状态和安全退出验收。

## 7. 输出与沟通格式

- 使用中文、结构清晰的Markdown；结论在前，细节在后。
- 明确区分：已确认需求、已实现、已测试、推断、未来范围和待用户确认。
- 不要把“无需API Key”写成“完全离线”。
- 不要把静态测试写成真实Luna成功；涉及AI必须分别报告登录、模型列表、真实图片识别和人工确认边界。
- 修改完成时应列出：改了什么、正式启动路径、测试结果、数据是否被触碰、遗留问题。
- 路径必须指向唯一正式目录，不得把旧版备份、release-ui-update或临时目录当成入口。

## 8. 已经完成的工作

- 完成React + TypeScript / FastAPI / SQLite项目和Alembic迁移。
- 完成Company、FC、Platform、Fee Plan、Client、Sub Account基础资料。
- 完成账户级Contribution/Withdrawal、Balance Snapshot与附件归档。
- 完成同版式eMPF JPG/PNG/PDF本地OCR、原文件归档、哈希/语义重复、复核、账户匹配和快照确认。
- 完成ChatGPT管理登录、固定Luna单次识别、结构化校验、OCR/AI冲突与人工复核边界。
- 完成账户级Beginning/Closing、多账户组合HWM结算、Finalize/Void与HWM继承。
- 完成Invoice Draft/Issue/Void、连续编号、中英文PDF和多笔Payment。
- 完成Dashboard、FC统计、内部Excel导出、中文/英文Settlement PDF。
- 完成本地完整备份与延迟恢复机制。
- 完成Windows PyInstaller onedir一键包，内置Tesseract、Codex CLI和前端。
- 完成页面视觉精简和窄屏适配，三个指定重复提示已删除。
- 完成页面内安全退出和前端入口no-store缓存策略。
- 2026-08-26建立Git交接基准前重新运行后端测试：91/91通过；仅有Starlette/httpx兼容性弃用警告；前端生产构建通过。

## 9. 重要决策及理由

- 本地SQLite而不是早期工作簿中的“联网数据库”：第一版强调本机隐私、低部署成本和未来可迁移。
- Sub Account独立HWM：避免不同账户盈亏互相抵销；先逐账户计算Service Fee，再在客户Invoice汇总。
- Beginning、Closing和HWM都保持账户级来源与连续链，确保凭证和收费可追溯。
- 当前收费计划就是工作簿中的利润20%；通用FeePlan字段只作为未来扩展空间，不能覆盖当前业务事实。
- 首次HWM人工确认：系统没有足够依据安全推断初始高水位线。
- OCR + Luna双通道、人工最终确认：提高识别效率但不牺牲财务可靠性。
- ChatGPT管理登录而不是普通API：满足用户不新增API费用的明确要求；但运行时受订阅额度和模型可用性影响。
- Luna失败不升级：用户明确选择可控、可审计的单模型流程，避免结果漂移和额外消耗。
- Issue时才分配Invoice编号且Void不复用：保证编号审计连续性。
- Service Fee为0不出付款Invoice：避免产生没有应付款的账单。
- 独立构建后提升正式目录：解决旧包运行、端口占用、覆盖损坏和用户误启动的问题。
- 页面内安全退出：解决只关浏览器后后台残留、必须任务管理器的问题。

## 10. 已被否定的方案

- 只写PRD/技术文档，不交可运行MVP。
- 任意版式OCR、网页自动登录抓取、历史批量导入作为第一版。
- OCR或AI结果直接正式入账。
- 删除本地OCR，只依赖AI。
- 未匹配账户自动成为可结算正式账户，或反过来完全拒绝导入。
- 非季末余额直接作为季度Closing。
- 首次HWM自动等于Beginning或开始管理金额。
- Luna冲突后升级Terra/Sol、调用第二模型、自动重试或AI自动胜出。
- 额外购买普通OpenAI API/API Key。
- Service Fee为0仍生成付款Invoice。
- Issued Invoice原地修改、Void号码复用、Invoice序号每月重置或全公司共享一个序列。
- 仅英文UI或全站双语切换；最终为中文主界面+英文财务术语。
- 日常通过任务管理器关闭程序。
- 保留用户截图红圈中的重复云端/升级提示块。

## 11. 当前正式状态（2026-08-26）

- 正式版本：MVP 0.2.1修复版。
- 唯一正式运行目录：`D:\-yunyun\OneDrive\Desktop\财务系统制作\release\FinancialFeeSystem`。
- 正式启动：`release\FinancialFeeSystem\启动金融计划收费系统.bat` 或同目录EXE。
- 不要启动：`release\旧版备份_请勿启动_20260824`。
- 根目录旧ZIP `金融计划收费计算系统_MVP_0.1.0_Windows.zip`是旧0.1，不得交付。
- EXE SHA-256：`14418DBFE5D4B223011EA546AF31F284A8822DC30D6083C0329066C4469C0B69`。
- 正式目录：340个文件，610,371,384字节。
- 前端正式资源：`index-C6R3PxdL.js`；三个已删除提示文本命中0。
- 最终实机验收曾确认：`/api/health`版本0.2.1、Luna `ready`、`gpt-5.6-luna`可用、ChatGPT管理登录、安全退出返回成功、主程序退出、应用自有Codex剩余0。
- 当前审计时系统未运行，8000未监听。
- 当前数据根：`D:\-yunyun\OneDrive\Desktop\财务数据`。
- 主数据库存在；当前有1个statement import，attachments/output/backups为空，没有pending restore，也没有实际恢复点。
- 数据根位于OneDrive目录，是否同步到云端取决于OneDrive设置；“服务只监听本机”不等于“文件绝不会被OneDrive同步”。
- 正式包中的 `financial_system_config.json` 是当前电脑专属路径。制作通用换机运行包时必须只在副本中移除它，让新电脑首次启动重新选择数据目录；不要删除当前正式包中的配置。

## 12. 尚未完成或仅预留的范围

明确不在当前MVP中，不能写成已完成：

- Defer延付利息计算。
- 自动邮件发送PDF。
- 用户登录、角色、权限和多人协作。
- 公司服务器/局域网部署。
- 任意版式OCR、多页PDF、历史批量导入、网页登录抓取。
- 管理员上传Excel新版模板及模板版本控制。
- 定时自动备份、备份加密、数据保留/删除策略。
- 完整AuditEvent前端和全量业务操作审计。
- CI、Git历史、浏览器E2E自动化报告。

这些只是当前MVP范围外；除用户明确承诺的未来服务器迁移外，不得自行声称它们一定是下一期。

## 13. 已知实现缺口与已确认目标（截至2026-08-27）

1. Settlement/HWM粒度：已改为每个Sub Account独立计算并继承HWM；组合容器只汇总各账户结果，旧记录保留为`LEGACY_GROUP_HWM`。
2. Invoice聚合：当前一条Settlement生成一张Invoice；目标是同一客户季度账单汇总多个独立账户明细，可跨Platform但不混算账户HWM。
3. Closing资格：已强制关联属于该账户、日期匹配且符合资格的Snapshot，不再接受手填Closing。
4. Beginning来源：已在首次结算强制明确期初Snapshot，后续自动关联该账户上期Closing Snapshot并继承金额。
5. 凭证强制性：Contribution、Withdrawal、Beginning和Closing已在Settlement Finalize前强制检查；Payment凭证硬前置尚未实现。
6. 重复入账：精确哈希和同账户同日快照可阻断常见重复，但部分语义重复目前只警告。
7. Payment和更正：当前支持部分付款且缺少退款/差额/错账更正流程；目标只保留未付/已付，并按2.1节记录公司承担差额或退款。
8. FC统计：当前包含Invoice已收/未收；目标仅保留两类客户数和Service Fee。
9. 多数基础资料、流水、快照、附件目前缺少完整编辑/删除/更正流程。
10. Company Logo字段存在，但没有上传/设置界面。
11. 本阶段Starting/Closing Date仍是组合容器级字段；账户中途加入/退出是否采用账户各自日期，等待用户确认。

当前GitHub源码为0.2.3开发基线，尚未正式发布；2026-08-27重新验证后端111/111、前端TypeScript和Vite生产构建通过，并完成两页合成PDF逐页传输与结算PDF视觉渲染检查。第11节保留的是2026-08-26正式运行包历史状态，不能与当前GitHub开发状态混为一谈。

## 14. 敏感与不可随意打包的内容

- `客人账单.jpg`含客户姓名、账户号和财务信息。本次用户明确要求在自己的私有换机开发交接包中保留原件且无需匿名化；仍不得放入Git、公开演示或发送给无权接触该资料的人。
- `D:\-yunyun\OneDrive\Desktop\财务数据`是真实业务数据根，不能与源码包混合；迁移时使用系统完整备份并通过受保护方式传输。
- ChatGPT/Codex登录令牌不在发布包或源码中，也不得复制；新电脑由用户本人重新浏览器授权。
- `.venv`、`frontend/node_modules`、`backend/build`、`tmp`、旧release和测试数据库不是源码交接必需内容，不应原样压入开发包；原始账单和验证资料则按用户本次明确授权保留。

## 15. 新会话接下来首先应该做什么

任何新任务开始前，按顺序执行：

1. 只读阅读：`AGENTS.md`、`README.md`、`docs/项目说明书.md`、`docs/CHANGELOG.md`，再按任务阅读用户手册、技术说明、AI说明、构建清单及本历史文档。
2. 确认唯一正式入口是`release/FinancialFeeSystem`，绝不启动旧版备份或0.1 ZIP。
3. 检查8000端口和FinancialFeeSystem进程路径；如果系统正在运行，先用页面“安全退出系统”，再做构建或发布操作。
4. 只读核对正式EXE哈希、`/api/health`版本、真实index/assets、三个旧提示命中0和当前数据根；查看Luna状态不等于触发图片上传。
5. 明确用户本轮要解决的是“新功能、业务规则确认、Bug、UI还是迁移”，再提出最小变更方案。
6. 修改源代码后先在独立release名称下构建，运行全部测试，并按风险做正式EXE、浏览器、Excel/PDF、Luna和退出验收。
7. 未建立当前业务数据备份前，不执行数据库迁移、恢复、删除、批量改写或服务器化。

建议新会话的第一句回复应说明：已经读取交接文档；先进行只读状态核查；不会触碰真实财务数据或启动Luna图片上传；核查完成后再报告下一步。

## 16. 关键路径速查

```text
项目根目录
D:\-yunyun\OneDrive\Desktop\财务系统制作

正式启动
D:\-yunyun\OneDrive\Desktop\财务系统制作\release\FinancialFeeSystem\启动金融计划收费系统.bat

当前真实数据根
D:\-yunyun\OneDrive\Desktop\财务数据

核心说明
README.md
docs\用户操作手册.md
docs\技术与验收说明.md
docs\ChatGPT Pro辅助识别说明.md
packaging\构建清单.txt

核心源码
backend\app\models.py
backend\app\services\calculation.py
backend\app\services\statement_parser.py
backend\app\services\codex_app_server.py
backend\app\services\shutdown.py
frontend\src\App.tsx
frontend\src\pages\ImportsPage.tsx

测试与构建
backend\tests
scripts\setup-dev.ps1
scripts\build-windows.ps1
```
